const BaseComponent = require('@janiking-org/base-component');
const dom = require('@janiking-org/dom');
const on = require('@janiking-org/on');
const dates = require('@janiking-org/dates');
const keys = require('@janiking-org/key-nav');
const nodash = require('@janiking-org/no-dash');

module.exports = {
    BaseComponent,
    dom,
    on,
    dates,
    keys,
    nodash,
};
